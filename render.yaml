services:
  - type: web
    name: aeo-fpa-agent
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 04_api_server.py
    plan: free
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: PORT
        value: 10000
